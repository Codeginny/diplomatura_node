<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1><%= title %></h1>
        <a href="/" class="btn">Volver al Inicio</a>
    </header>
    <main>
        <!-- Acción dinámica: si es edición, incluye el ID en la ruta -->
        <form action="<%= typeof hero !== 'undefined' && hero ? '/heroes/' + hero._id + '/edit' : '/heroes' %>" method="POST">
            <label for="nombreSuperHeroe">Nombre del Superhéroe:</label>
            <input type="text" id="nombreSuperHeroe" name="nombreSuperHeroe" value="<%= typeof hero !== 'undefined' && hero ? hero.nombreSuperHeroe : '' %>" required>
            
            <label for="nombreReal">Nombre Real:</label>
            <input type="text" id="nombreReal" name="nombreReal" value="<%= typeof hero !== 'undefined' && hero ? hero.nombreReal : '' %>" required>
            
            <label for="nombreSociedad">Sociedad:</label>
            <input type="text" id="nombreSociedad" name="nombreSociedad" value="<%= typeof hero !== 'undefined' && hero ? hero.nombreSociedad : '' %>">
            
            <label for="edad">Edad:</label>
            <input type="number" id="edad" name="edad" value="<%= typeof hero !== 'undefined' && hero ? hero.edad : '' %>">
            
            <label for="planetaOrigen">Planeta de Origen:</label>
            <input type="text" id="planetaOrigen" name="planetaOrigen" value="<%= typeof hero !== 'undefined' && hero ? hero.planetaOrigen : '' %>">
            
            <label for="debilidad">Debilidad:</label>
            <input type="text" id="debilidad" name="debilidad" value="<%= typeof hero !== 'undefined' && hero ? hero.debilidad : '' %>">
            
            <label for="poder">Poderes (separados por comas):</label>
            <input type="text" id="poder" name="poder" value="<%= typeof hero !== 'undefined' && hero ? hero.poder.join(', ') : '' %>">
            
            <label for="habilidadEspecial">Habilidad Especial:</label>
            <input type="text" id="habilidadEspecial" name="habilidadEspecial" value="<%= typeof hero !== 'undefined' && hero ? hero.habilidadEspecial : '' %>">
            
            <label for="aliado">Aliados (separados por comas):</label>
            <input type="text" id="aliado" name="aliado" value="<%= typeof hero !== 'undefined' && hero ? hero.aliado.join(', ') : '' %>">
            
            <label for="enemigo">Enemigos (separados por comas):</label>
            <input type="text" id="enemigo" name="enemigo" value="<%= typeof hero !== 'undefined' && hero ? hero.enemigo.join(', ') : '' %>">
          
            <!-- Botón dinámico para creación o edición -->
            <button type="submit"><%= typeof hero !== 'undefined' && hero ? 'Actualizar' : 'Agregar' %></button>
        </form>
    </main>
 
</body>
</html>
